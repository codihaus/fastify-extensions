// Declares manifest id "hook-basic", colliding with the local fixture of the same id.
// Local must win: this module variant should be de-duplicated away.
export default function hookBasicMod(hook) {
	hook.filter('items.create', (payload) => ({ ...payload, fromModule: true }));
}
