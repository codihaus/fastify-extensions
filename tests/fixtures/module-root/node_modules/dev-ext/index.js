// A valid extension listed only under devDependencies -> module scan must NOT pick it up.
export default function devExt(hook) {
	hook.filter('dev.event', (payload) => ({ ...payload, fromDev: true }));
}
