// A valid module-installed hook extension, discovered via the host's dependencies.
export default function modExt(hook) {
	hook.filter('mod.event', (payload) => {
		return { ...payload, fromModule: true };
	});
}
